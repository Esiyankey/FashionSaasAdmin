# Billing (Phase 2)

Not implemented yet. Owns subscription plan, invoices, and payment method management for a
business. `types/business.ts`'s `Business.plan` (`"starter" | "growth" | "enterprise"`) is the
likely seed for plan-related UI here. Follow the `components/ hooks/ services/ types.ts` shape
used by `features/dashboard`.

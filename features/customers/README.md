# Customers (Phase 2)

Not implemented yet. Owns the customer directory and customer-level order/order-history views.
Follow the `components/ hooks/ services/ types.ts` shape used by `features/dashboard`. The
storefront already defines a `Customer` type (`types/customer.ts` in FashionSaas) — check whether
it can be reused as-is before redefining it here.
